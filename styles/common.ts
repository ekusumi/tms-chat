import { StyleSheet } from "react-native";

const common = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  viewHeader: {
    backgroundColor: "#eee",
    width: "100%",
    height: 120,
  },
  viewNavigation: {
    height: 50,
    marginTop: 50,
    flexDirection: "row",
  },
});

export default common;
