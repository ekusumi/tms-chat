import AmityService from "../services/AmityService";

const useGetImageFile = async (fileId: string) => {
  // let image = await AmityService.getImageFile(fileId);
};

export default useGetImageFile;
